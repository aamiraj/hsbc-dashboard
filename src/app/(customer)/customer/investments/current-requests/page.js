import React from "react";

const CurrentRequests = () => {
  return (
    <div className="w-full h-screen flex items-center justify-center ">
      <p className="text-center">No current history.</p>
    </div>
  );
};

export default CurrentRequests;
